# Set these variables in your shell; the app does not automatically load this file.
DATABASE_URL=postgresql://username:password@hostname:5432/planner
# Connections currently require a PostgreSQL server with SSL enabled.
